#!/usr/bin/env bash

latlong_test() {
	# Stub the `needs` helper so latlong doesn't try to validate dependencies
	needs() { :; }
	export -f needs

	assert "$(LATLONG_OVERRIDE='40.123,-73.456' latlong)" == "40.123,-73.456"

	unset -f needs
}

latlong_test

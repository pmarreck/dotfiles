#!/usr/bin/env bash

if [ -z "${DOCUMENTS:-}" ]; then
	echo "link_AGENTS_here: DOCUMENTS is not set or is empty" >&2
	exit 1
fi

ln -s "$DOCUMENTS/Obsidian Vaults/Peter Marreck/AGENTS_concise.md.md" AGENTS.md
